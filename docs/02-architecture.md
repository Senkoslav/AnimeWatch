# 02. Архитектура

## Топология

```
Телеграм-канал (мастера 3.5 ГБ)
        │  MTProto, отдельный аккаунт-воркер
        ▼
  Worker (VPS, Node + BullMQ)  ──►  Bunny Stream  ──►  Bunny CDN (HLS)
        │                                │                   │
        │ статус задачи                  │ webhook            │ .m3u8 + сегменты
        ▼                                ▼                   ▼
   PostgreSQL  ◄────────────────  Next.js (Vercel)  ────►  Браузер зрителя
```

Next.js никогда не стоит на пути видеотрафика. Он отдаёт HTML и подписанный URL
плейлиста, дальше браузер общается с CDN напрямую.

## Компоненты

**Web (Vercel).** Next.js App Router, SSR/ISR для публичных страниц, админка —
динамическая. Vercel запрещает раздачу видео через свою сеть, поэтому любой
код, который проксирует видеопоток, — ошибка проектирования.

**База (Neon/Supabase, Postgres).** Prisma. Connection pooling обязателен:
serverless-функции открывают соединения агрессивно.

**Воркер (VPS, Hetzner/Aeza).** Отдельный репозиторий-пакет в монорепо,
собственный процесс. Очередь на BullMQ поверх Redis. Делает ровно одно:
забирает мастер из телеграма и отдаёт в Bunny, обновляя статус в БД.

**Bunny Stream.** Транскодинг в лестницу качеств, хранение, раздача HLS,
токен-аутентификация, вебхуки о готовности. Кодирование не тарифицируется
отдельно, платим за хранение и трафик — актуальные цифры проверять на сайте.

## Маршруты

```
/                         главная: свежие серии
/catalog                  каталог с фильтрами (searchParams = состояние)
/anime/[slug]             страница тайтла
/anime/[slug]/[episode]   просмотр
/team                     команда и роли
/news, /news/[slug]       новости
/search                   поиск
/me                       закладки и «продолжить»
/admin/*                  админка (роль ADMIN | EDITOR)
/dmca, /terms, /privacy   обязательные страницы

/api/webhooks/bunny       вебхук о готовности видео
/api/auth/telegram        проверка подписи Telegram Login
/api/playback/[episodeId] выдача подписанного URL плейлиста
/api/progress             сохранение позиции (батчем, не каждую секунду)
```

## Кеширование

- Страница тайтла — ISR, `revalidate: 300`, плюс точечная ревалидация тега из
  админки после правки. В Next 16 это `updateTag('title:<id>')` внутри server
  action: редактор сразу видит свою правку. `revalidateTag` теперь требует второй
  аргумент (`revalidateTag(tag, 'max')`, stale-while-revalidate) и нужен только
  вне server actions, например в вебхуке Bunny.
- Каталог — динамический рендер: фильтры в `searchParams`, а их чтение отключает
  статику. Кеш данных без Cache Components возможен только через
  `unstable_cache`, который в Next 16 заменён на `use cache`; пока запросы идут в
  базу на каждый показ (они по индексам и небольшие). Нечистые адреса
  (пустые поля формы, мусор) редиректятся на канонический URL из
  `lib/catalog/params.ts`.
- Поиск — динамический рендер по той же причине, что каталог: запрос в
  `searchParams`. Канонический адрес собирается `lib/canonical.ts`; кодирование
  там и в `searchHref` должно совпадать, иначе редирект на канонический адрес
  зациклится на запросе из двух слов.
- Главная — `revalidate: 60`.
- Страница просмотра — динамическая (`force-dynamic`): в HTML лежит подписанный
  URL, ему нельзя в кеш. Скрытие тайтла на ней действует сразу.
- `/me`, админка — `no-store`.
- Теги: `titles`, `title:<id>`, `episodes:<titleId>`, `news`.

## Безопасность

- Ссылки на плейлист подписываются на сервере и живут ограниченное время.
  Срок должен покрывать длину серии с запасом, иначе плеер отвалится на середине.
- Hotlink protection и разрешённые домены в Bunny включаются сразу, не «потом».
  Иначе каталог утащит чужой агрегатор, а трафик оплатим мы.
- Telegram Login проверяется по HMAC от токена бота, с проверкой `auth_date`.
  Данные виджета без проверки подписи — не авторизация.
- Роли: `USER | EDITOR | ADMIN`. Проверка роли в каждом server action первым
  делом, не в UI.
- Rate limit на `/api/playback` и поиск.
- Секреты Bunny и сессия телеграм-аккаунта живут только на воркере и на сервере.

## Env

```
DATABASE_URL             # через пулер: рантайм и next build (ISR-страницы рендерятся на сборке)
DIRECT_URL               # напрямую: миграции и prisma studio
BUNNY_STREAM_API_KEY
BUNNY_LIBRARY_ID
BUNNY_CDN_HOSTNAME
BUNNY_TOKEN_KEY          # подпись playback-URL
BUNNY_WEBHOOK_SECRET
TELEGRAM_BOT_TOKEN       # проверка подписи логина
TG_API_ID / TG_API_HASH / TG_SESSION   # только воркер, worker/.env
TG_ARCHIVE_CHAT          # только воркер: id архивного канала для tg:whoami
REDIS_URL                # только воркер
NEXT_PUBLIC_SITE_URL
```

## Версии

Сверено по npm 2026-09-16. Обновляя мажорную версию, проверь причины из таблицы.

| Что | Версия | Почему |
|---|---|---|
| Node | 24 LTS (`.nvmrc`, `engines`) | Vercel работает на LTS; Vitest 5 не поддерживает нечётные 25.x |
| Next.js | 16.3 | `next dev` по умолчанию на Turbopack; `next lint` убран, линт через `eslint .` |
| TypeScript | ~6.0 | в TS 7 (нативный) нет старого compiler API: на нём не работают проверка типов в `next build` и typescript-eslint (`<6.1.0`) |
| ESLint | 9.x | плагины из eslint-config-next (react, import, jsx-a11y) объявляют peer до `^9` |
| Tailwind CSS | 4.x | конфиг в CSS через `@theme` в `app/globals.css`, без `tailwind.config` |
| Vitest / Playwright | 5 / 1.63 | — |
| Video.js | 10.0.0-rc.2, версия закреплена точно | RC: обновлять осознанно, прогоняя `tests/e2e/player.spec.ts` |
| Prisma | 7.10 | 8 на 2026-09-16 только RC: `@prisma/client@8` не опубликован, CLI переписан. Переходить после стабильного релиза |

## Решения и их причины

| Решение | Почему так, а не иначе |
|---|---|
| Bunny Stream, не свой ffmpeg | 4K-энкод на CPU — часы на серию; нужен GPU и обслуживание |
| Bunny, не Cloudflare Stream | у CF тарификация по минутам, на нашем объёме выходит дороже |
| Телеграм как архив | бесплатно и уже используется студией; но только как холодное хранилище |
| Video.js 10, не Vidstack | Vidstack влился в Video.js 10: у Vidstack с июня 2026 нет релизов, 1.x так и не стал `latest`; у v10 RC и частые релизы. Те же headless-компоненты, hls.js-адаптер, выбор качества, горячие клавиши, русская локаль |
| Postgres, не MongoDB | связи тайтл↔серия↔роль↔актёр реляционные по природе |
| Telegram Login, не email | вся аудитория и так в телеграме; email-регистрация — лишний барьер |
