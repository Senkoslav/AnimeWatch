/**
 * Панель каталога: фон страницы --bg, блоки --surface с рамкой в 1px (docs/04, «глубина через
 * surface и line»). Один на выдачу и фильтры, чтобы у соседних панелей не разъезжались отступы.
 */
export const CATALOG_PANEL = "rounded-md border border-line bg-surface p-4 md:p-6";
